const {ethers} = require("hardhat");
const {expect} = require("chai");


describe("ChainSphereTokenICO", async ()=> {
    let owner, addr1, addr2;
    let ChainSphereTokenICO, ico;
    let USDT, usdt;
    const decimal = 18;
    const totalSupply = ethers.BigNumber.from() 
})